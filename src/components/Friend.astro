---
import type { FriendObject } from "types";

export interface Props {
  friend: FriendObject;
}

const { friend } = Astro.props;
---
<div>
  <h3><a href={friend.link}>{friend.name}</a></h2>
  <p>
    {friend.description}
  </p>
</div>
